# Backend-Api-Integration
Built a RESTful backend using Django REST Framework to manage all CRUD operations for members. Exposed secure API endpoints that handle data validation, serialization, and communication with the React frontend.
